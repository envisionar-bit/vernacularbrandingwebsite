/* Collective Canvas: paste the deployed web-app URL of backend/collective-canvas.gs between the quotes.
   Leave empty to keep names on the visitor's own device only (preview). */
window.VB_CANVAS_ENDPOINT = "";
